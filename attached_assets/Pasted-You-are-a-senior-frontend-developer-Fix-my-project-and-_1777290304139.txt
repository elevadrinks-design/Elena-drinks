You are a senior frontend developer.

Fix my project and convert it into a CLEAN, STATIC, PRODUCTION-READY web app.

==================================================
IMPORTANT CONTEXT

My project was incorrectly created as a Node.js + TypeScript backend app.

It currently includes:
- src/app.ts
- package.json
- node_modules
- pino-http
- backend/server code

This is WRONG.

I want a pure frontend app.

==================================================
YOUR TASK

1. DELETE ALL BACKEND CODE:

Remove:
- src/ folder
- all .ts files
- package.json
- package-lock.json / pnpm-lock.yaml / yarn.lock
- node_modules
- any server or API code
- any use of pino-http or express

==================================================
2. CREATE CLEAN STRUCTURE

Only keep / create:

- index.html
- style.css
- script.js
- README.md

==================================================
3. STRICT RULES

- Use ONLY HTML, CSS, and Vanilla JavaScript
- NO TypeScript
- NO Node.js
- NO backend
- NO frameworks
- NO build tools

==================================================
4. VERCEL COMPATIBILITY (VERY IMPORTANT)

Make sure:

- index.html is in root
- CSS and JS use RELATIVE paths:
  (style.css, script.js)
- No absolute paths like /style.css
- No localhost URLs
- No imports from node_modules
- No fetch calls to backend

The app must run by opening index.html directly.

==================================================
5. BUILD THE APP FEATURES

Create a mobile-first ordering app:

APP NAME:
Eleva Drinks 💪

FEATURES:

- Dynamic product menu using JavaScript array
- Categories (Protein Shakes, Summer Drinks, Snacks)
- Add to cart (+ / - buttons)
- Live total price
- Sticky bottom cart bar
- Notes textarea
- WhatsApp order integration

==================================================
6. ADMIN PANEL

Add admin login button (top-right ⚙️)

Login:
ID: eleva1
Password: eleva123drinks

Admin can:
- Add / edit / delete products
- Change price
- Set discount %
- Upload image (Base64)
- Toggle availability
- Change WhatsApp number

==================================================
7. DISCOUNT SYSTEM

Each product supports discount %.

Show:
- Original price (crossed)
- Discounted price

==================================================
8. IMAGE UPLOAD

Allow admin to upload image:
- Convert to Base64
- Save in localStorage

==================================================
9. OFFLINE SALES TRACKER

Admin panel must include:

- Add manual sale
- Select product
- Enter quantity
- Save sale
- Show daily total
- Delete incorrect entries

==================================================
10. DATA STORAGE

Use localStorage for:
- Products
- Settings
- Sales

==================================================
11. FINAL REQUIREMENTS

- Fully working without errors
- Works on mobile
- Fast loading
- Clean UI (dark theme, green accents)
- No console errors

==================================================
GOAL

The final result must be:

- A real business-ready app
- Deployable on Vercel instantly
- ZERO build errors
- ZERO backend dependency